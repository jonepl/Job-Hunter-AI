# LLM Provider
openai
anthropic

# Pre-filter (Gemini Flash-Lite)
google-genai

# Web API (FastAPI driving adapter — serves the SPA + JSON API)
fastapi
uvicorn[standard]
python-multipart  # multipart/form-data uploads (POST /api/resume, W5)

# Environment variable management
python-dotenv

# Web scraping
playwright
beautifulsoup4
requests

# Resume parsing
pypdf2

# Document generation (tailored resume + cover letter .docx)
python-docx

# Testing
pytest
pytest-asyncio

# Utilities
lxml

# Scheduling
apscheduler
pytz
