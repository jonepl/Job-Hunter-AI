Build the Docker container.

- Follow `.claude/skills/docker/SKILL.md`.
- Run: `docker compose build`
- Confirm the build completes without errors and that Playwright browser
  binaries install successfully inside the container.
